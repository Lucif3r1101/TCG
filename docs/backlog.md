# Product Backlog

## Email/Auth
- [ ] Add SMTP fallback/provider mode for password reset emails.
- [ ] Implement env-driven mail adapter (`MAIL_PROVIDER=resend|smtp`).
- [ ] Add Gmail SMTP beta configuration path (no custom domain) for early production testing.
- [ ] Keep Resend path as primary option when domain is verified.
- [ ] Update deployment docs with SMTP env variables and setup checklist.
